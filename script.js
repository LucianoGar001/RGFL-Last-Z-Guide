const grid=document.getElementById('guideGrid');const search=document.getElementById('search');const filters=document.getElementById('filters');const viewer=document.getElementById('viewer');const viewerImage=document.getElementById('viewerImage');const viewerTitle=document.getElementById('viewerTitle');const closeViewer=document.getElementById('closeViewer');const emptyState=document.getElementById('emptyState');let activeFilter='All';let guides=[];function esc(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function render(){const q=search.value.trim().toLowerCase();const filtered=guides.filter(g=>{const inFilter=activeFilter==='All'||g.category===activeFilter;const haystack=[g.title,g.subtitle,g.category,...g.tags,...g.key].join(' ').toLowerCase();return inFilter&&(!q||haystack.includes(q))});grid.innerHTML=filtered.map(g=>`<article class="guide-card"><div class="guide-visual"><img src="assets/web/${g.id}.webp" alt="${esc(g.title)} infographic preview" loading="lazy"><button class="open-guide" type="button" data-guide="${g.id}">View full guide</button></div><div class="card-body"><div class="card-top"><div><h3>${esc(g.title)}</h3><p class="subtitle">${esc(g.subtitle)}</p></div><span class="badge">${esc(g.category)}</span></div><div class="key-title">KEY INFORMATION</div><ul class="key-list">${g.key.map(item=>`<li>${esc(item)}</li>`).join('')}</ul><div class="tags">${g.tags.map(t=>`<span class="tag">${esc(t)}</span>`).join('')}</div></div></article>`).join('');emptyState.hidden=filtered.length!==0}function openGuide(id){const g=guides.find(x=>x.id===id);if(!g)return;viewerTitle.textContent=g.title;viewerImage.src=`assets/web/${g.id}.webp`;viewerImage.alt=`${g.title} full infographic`;if(typeof viewer.showModal==='function')viewer.showModal();else viewer.setAttribute('open','')}grid.addEventListener('click',e=>{const btn=e.target.closest('[data-guide]');if(btn)openGuide(btn.dataset.guide)});filters.addEventListener('click',e=>{const btn=e.target.closest('[data-filter]');if(!btn)return;activeFilter=btn.dataset.filter;filters.querySelectorAll('.filter').forEach(b=>b.classList.toggle('active',b===btn));render()});search.addEventListener('input',render);closeViewer.addEventListener('click',()=>viewer.close());viewer.addEventListener('click',e=>{if(e.target===viewer)viewer.close()});document.getElementById('printBtn').addEventListener('click',()=>window.print());fetch('guides.json').then(r=>r.json()).then(data=>{guides=data;render()}).catch(()=>{grid.innerHTML='<div class="empty">Could not load guide data.</div>'});